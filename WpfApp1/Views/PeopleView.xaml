<UserControl x:Class="WpfApp1.Views.PeopleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и поиск -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="👥 Управление пользователями" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Width="250" Height="25" Margin="10,0,0,0" 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Поиск по ФИО или логину"/>
        </StackPanel>

        <!-- Форма ввода -->
        <GroupBox Grid.Row="1" Header="Данные пользователя" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
                    <TextBlock Text="ФИО:" FontWeight="Bold"/>
                    <TextBox Text="{Binding SelectedPerson.Фио, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,5"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">
                    <TextBlock Text="Логин:" FontWeight="Bold"/>
                    <TextBox Text="{Binding SelectedPerson.Логин, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,5"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="5">
                    <TextBlock Text="Пароль:" FontWeight="Bold"/>
                    <TextBox Text="{Binding SelectedPerson.Пароль, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,5"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
                    <TextBlock Text="Роль:" FontWeight="Bold"/>
                    <ComboBox Text="{Binding SelectedPerson.Роль, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,5">
                        <ComboBoxItem Content="родитель"/>
                        <ComboBoxItem Content="оператор"/>
                        <ComboBoxItem Content="администратор"/>
                        <ComboBoxItem Content="медработник"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="0" Margin="5">
                    <TextBlock Text="Телефон:" FontWeight="Bold"/>
                    <TextBox Text="{Binding SelectedPerson.Телефон, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,5"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="1" Margin="5">
                    <TextBlock Text="Email:" FontWeight="Bold"/>
                    <TextBox Text="{Binding SelectedPerson.Почта, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,5"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.Column="0" Margin="5">
                    <TextBlock Text="Паспорт:" FontWeight="Bold"/>
                    <TextBox Text="{Binding SelectedPerson.Паспорт, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,5"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Таблица данных -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding PeopleList}" 
                  SelectedItem="{Binding SelectedPerson}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding Фио}" Width="150"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Логин}" Width="100"/>
                <DataGridTextColumn Header="Роль" Binding="{Binding Роль}" Width="100"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Телефон}" Width="100"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Почта}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="💾 Добавить" Command="{Binding AddCommand}" Width="100" Margin="5" Padding="5"/>
            <Button Content="✏️ Изменить" Command="{Binding UpdateCommand}" Width="100" Margin="5" Padding="5"/>
            <Button Content="🗑️ Удалить" Command="{Binding DeleteCommand}" Width="100" Margin="5" Padding="5"/>
            <Button Content="🔄 Обновить" Command="{Binding LoadDataCommand}" Width="100" Margin="5" Padding="5"/>
        </StackPanel>
    </Grid>
</UserControl>