<UserControl x:Class="WpfApp1.Views.BookingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800"> <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📋 Бронирования" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <GroupBox Grid.Row="0" Header="Новое бронирование" Margin="0,30,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock Text="Ребёнок:"/>
                    <ComboBox ItemsSource="{Binding ChildrenList}" 
                              DisplayMemberPath="Фио" 
                              SelectedValuePath="Id"
                              SelectedValue="{Binding SelectedBooking.FkРебенка}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock Text="Смена (ID):"/>
                    <!-- Для простоты пока выбираем по ID смены -->
                    <ComboBox ItemsSource="{Binding SessionsList}" 
                              DisplayMemberPath="Id" 
                              SelectedValuePath="Id"
                              SelectedValue="{Binding SelectedBooking.FkСмена}"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <DataGrid Grid.Row="1" ItemsSource="{Binding BookingsList}" 
                  SelectedItem="{Binding SelectedBooking}"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ребёнок" Binding="{Binding FkРебенкаNavigation.Фио}" Width="150"/>
                <DataGridTextColumn Header="Смена (Начало)" Binding="{Binding FkСменаNavigation.ДатаНачала}" Width="120"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Статус}" Width="100"/>
                <DataGridTextColumn Header="Дата оформления" Binding="{Binding ДатаОформления}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="➕ Создать" Command="{Binding AddCommand}" Width="120" Margin="5"/>
            <Button Content="🔄 Обновить" Command="{Binding LoadDataCommand}" Width="100" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>